#ZIP Function --zip()-- Iterate over several iterables in parallel, producing tuples with an item from each one.
for item in zip([1, 2, 3], ['sugar', 'spice', 'everything nice']):
    print(item)
    
    
#BIN Function -- bin()-- Convert an integer number to a binary string prefixed with “0b”. The result is a valid Python expression. 
bin(3)
bin(-10)
bin(27)


#OCT Function -- oct() -- Convert an integer number to an octal string prefixed with “0o”. The result is a valid Python expression. 
oct(5)
oct(-56)
oct(7)


#Print factorial n (n is a parameter)
def cal_fact(n) :
    fact = 1
    for i in range (1, n=1):
        fact *= 1
        print(fact)
        
#END!!!

